var express = require("express");
var router = express.Router();

router.get("/events", (req, res, next) => {
    // implement eventbrite API to search based on the tag required and the location
    

});

module.exports = router;